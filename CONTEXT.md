# Jotway

Jotway 是一个 macOS 启动器：用户在快速记录面板输入一句话，Jotway 判断意图并交给一个 action 执行。输入不形成可浏览的收件箱或内容历史；意图反馈可在本地保留正文样本。

本文件只定义产品语言。当前行为与实现入口见 [docs/jotway.md](docs/jotway.md)。

## 输入

### 草稿（Draft）

快速记录面板中尚未执行的纯文字。Markdown 标记、路径和 URL 都只是普通字符；图片、文件附件和富文本样式不属于草稿。隐藏面板后仍可继续编辑；退出、崩溃或重启应用后清空。用户确认路由后，正文可能另存为本地意图反馈样本，但不会成为可浏览的内容记录。

_Avoid_：记录、临时记录、暂存任务、未完成任务

### 快速记录面板（Quick Record Panel）

由全局快捷键、菜单栏或应用入口唤起的输入窗口。

_Avoid_：处理面板、收件箱

### 编辑区（Editor）

快速记录面板中输入和修改多行纯文字的区域。

_Avoid_：面板（仅指输入区域时）

## 路由

### 意图识别（Intent Recognition）

根据草稿内容建议目标 action 的过程。识别结果只是建议；执行前用户仍可切换目标。

_Avoid_：自动执行、辅助建议

### Action / 内置 action

Jotway 可以路由并执行的目标能力。当前内置 action 包括 Apple Notes、Apple Reminders、Apple Calendar、Chrome 和 ChatGPT。

_Avoid_：插件、任务、Connector

### 默认 action（地板）

没有明确意图时优先接收草稿的可执行存入类 action。全部不可用时，面板提供 Notes 配置入口作为兜底。

_Avoid_：默认保存、收件箱兜底

### 配置入口（Setup）

为尚不可执行的 action 完成授权和目标配置的入口，当前仅 Notes 提供。配置不是执行：完成或取消后返回原草稿，完成后仍需再次按 Enter 才存入。

_Avoid_：自动保存、配置即提交

### 显式选择

用户在执行前主动切换目标 action。显式选择优先于意图识别结果。

## 执行

### 存入（Save）

由系统应用型 action 创建备忘录、提醒事项或日历事件。成功以目标应用确认创建为准。

### 执行失败恢复

Action 未成功受理时，Jotway 恢复原草稿，让用户修正后重试或改选目标。若用户已经开始新草稿，失败的旧提交会单独保留，直到用户显式恢复。
